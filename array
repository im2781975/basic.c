/* 5

5 6 7 8 9
<-5 6 7 8 9 */
#include<stdio.h>
int main()
{
    int N=10000,i,n;
    int arr[N];
    scanf("%d",&n);
    for(i=0;i<n;i++)
       scanf("%d",&arr[i]);
    
    for(i=0;i<n;i++)
     //printf("%d ",arr[1]+arr[2]);
     //printf("%d ",arr[0]);
      printf("%d ",arr[i]);
}
